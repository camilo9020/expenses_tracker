<div class="row">
  <div class="col-lg-12">
    <div class="page-header">
      <p> <%= link_to 'Regresar', :back, class: 'btn btn-default pull-right' %></p>
      <h2><%= @employee.first_name + ' ' + @employee.last_name %></h2>
    </div>
  </div>
</div>
<div class="row">
  <div class="col-lg-8">
    <h2 class="position">Position: <%= @employee.occupation %></h2>
    <p>Email: <%= @employee.email %></p>
    <p>Admin: <%=  @employee.admin? ? '<span class="label label-success">True</span>'.html_safe : '<span class="label label-danger">False</span>'.html_safe  %></p>
    <%= link_to 'Editar', edit_admin_employee_path, class: 'btn btn-default' %>
    <%= link_to 'Eliminar', admin_employee_path, method: :delete, data: {confirm: '¿Estas seguro de querer eliminar esta categoria?'}, class: 'btn btn-default' %>
  </div>
</div>
