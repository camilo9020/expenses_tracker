$('<%= j render 'form', title: 'Edit Category', url: admin_category_path, button_text: 'Update' %>').on('hidden.bs.modal', function (e) {
  $(this).remove();
  $('.modal-backdrop').remove();
}).modal();